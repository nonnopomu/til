## 今日の勉強記録
- **日付**：2026-01-14
- **テーマ**：Coffee Compass Cafes v0.1 要件定義・技術スタック見直し（地図なし）
- **今日決めたこと（要点）**：
  - 地図は使わず、検索は**都道府県単位（県のカフェ一覧）**に固定
  - v0.1は「新規登録時に診断（Yes/No）必須」→診断結果で**おすすめ順**表示、**人気順（公開ログ数）**にも切替
  - 店の新規登録は**ユーザー投稿**で増やす。必須は**店名・都道府県・住所**
  - 重複対策：`店名 + 都道府県 + 住所` の**重複候補を警告表示**（登録は可能）
  - 店の編集権限：**登録者＋管理者**／削除は**管理者のみ**
  - 豆傾向は未検証/検証済みを持ち、**検証済みは管理者手動**
  - レコメンド対象は**必須項目を満たす豆傾向がある店のみ**に絞る
  - 通報：自動非表示はしないが、**通報一覧UI（管理画面）**は作る
  - DBは期限切れで消えない構成にする（永続DB）
- **詰まりポイント / 対策**：
  - ユーザー投稿で荒れやすい → 編集/削除権限、通報、非表示運用（status）で運用可能に
  - 非公開ログの情報漏れ → 権限設計を最優先（特に画像表示）
  - データ不足で推薦が弱くなる → 推薦対象を条件で絞り、初期データを厚めにする前提
- **残タスク（次回やる）**：
  - P0のER（テーブル/カラム）確定
  - 画面一覧＋画面遷移（ユーザー/管理者）確定
  - おすすめスコア（タグ/焙煎度一致）の具体ルールを文書化
  - 画像制限（枚数/サイズ/形式）と非公開表示方針の確定・実装方針決め
