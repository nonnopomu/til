## ✅ 今日のサマリ
- 一言：パンくず（breadcrumbs）の定義と階層設計を理解しつつ、編集ページでの引数渡し漏れの原因を特定できた。あわせてテキスト挿入バグ（nil→String変換）の原因と、`||=`を使った簡潔なnil対応を習得した。
- 達成度：◯（原因→仕組み→対処まで整理できた）

## 🧩 やったこと（Issue/PRリンク）
- [x] Web：パンくず（breadcrumbs）定義の理解・編集ページ階層の設計・エラー原因の切り分け
- [x] Web：テキスト挿入バグ（`no implicit conversion of nil into String`）の原因特定とnil対応の実装方針決定

## 📚 学んだこと（メモ）
### Web（breadcrumbs / パンくず）
- `crumb :admin_tags do ... end`
  - `:admin_tags` はパンくず定義の“名前（ラベル）”。この行で初めて定義される
  - `link 'タグ', admin_tags_path` で **表示文字**と**リンク先URL**を設定する
- `parent :admin_dashboard`
  - パンくずの **親子関係（階層）** を指定する
  - `:admin_tags` の親を `:admin_dashboard` にすると `Home > タグ` の構造になる
- 編集ページの階層
  - 編集は一覧より深い階層なので、編集ページの `parent` は `:admin_tags` にする
  - 例：`Home > タグ > タグ編集`
- `breadcrumb :edit_admin_tag` のエラー原因
  - `crumb :edit_admin_tag do |tag| ... edit_admin_tag_path(tag) ... end` のように **引数 `tag` を受け取る定義**なのに、ビュー側で `@tag` を渡していなかったため `tag=nil` になっていた
  - 正：`breadcrumb :edit_admin_tag, @tag`（オブジェクトを渡す）
- `@tag` がビューに来る流れ
  - `/admin/tags/2/edit` → `params[:id]=2` → `Tag.find(params[:id])` で `@tag` セット → ビューで `@tag` が使える
  - `edit_admin_tag_path(@tag)` は `@tag.id` を使って `/admin/tags/2/edit` を生成できる

### Web（テキスト挿入バグ / nil対応）
- `no implicit conversion of nil into String`
  - `nil` を文字列として扱おうとして起きるエラー（例：`result << sentence.body`）
  - テキストブロック挿入直後で未入力だと `sentence.body` は `nil` になり得る
- nilのときの対処方針
  - `sentence.body` が `nil` の場合、代わりに空文字 `''`（または表示用の文字）を使うとエラー回避できる
  - `if sentence.body == nil ...` で分岐して埋める、という発想に到達
- 空文字とスペースの違い
  - `' '`（半角スペース）と `''`（空文字）は別物なので、意図に合わせて選ぶ必要がある
- `||=`（論理和代入）の理解
  - `a ||= b` は `a = a || b` と同じ意味
  - 左が `nil/false` のときだけ右が代入され、値があるときはそのまま
  - `||` の性質（左がtruthyなら左、`nil/false`なら右）を確認して理解できた
- 具体的な書き方のイメージ
  - `sentence.body ||= ''` で「nilなら空文字」を1行で表現できる（if文より簡潔）
